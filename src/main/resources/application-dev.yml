server:
  port: 8080

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://dev-codeit-4.cl6yoym4c9bq.ap-northeast-2.rds.amazonaws.com:3306/
    username: root
    password: Codeit4team!

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

mybatis:
  mapper-locations: classpath*:codeit/sqlmap/mapper/*-mapper.xml
  configuration:
    jdbc-type-for-null: null
    map-underscore-to-camel-case: true
  type-handlers-package: come.codeit.server.config